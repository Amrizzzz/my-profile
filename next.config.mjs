/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
          {
            hostname: 'al-mustofa.vercel.app',
          },
          {
            hostname: 'al-mustofa.vercel.app',
          },
        ],
      },
};

export default nextConfig;
